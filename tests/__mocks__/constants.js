'use strict';

// Mock des constantes pour les tests
module.exports = {
  // Timeouts
  HTTP_TIMEOUT_MS: 8000,
  UCM_TIMEOUT_MS: 8000,
  ODOO_TIMEOUT_MS: 8000,
  UCM_RECONNECT_DELAY_MS: 3000,
  UCM_RECONNECT_MAX_DELAY_MS: 60000,
  CALL_POLLING_INTERVAL_MS: 3000,
  CDR_SYNC_INITIAL_DELAY_MS: 30000,
  SPAM_CHECK_TIMEOUT_MS: 5000,

  // Intervalles et délais
  SESSION_TTL_MS: 8 * 60 * 60 * 1000,
  WEBHOOK_EVENT_TTL_MS: 30 * 24 * 60 * 60 * 1000,
  CONTACT_CACHE_TTL_MS: 300000,
  SIRENE_CACHE_TTL_MS: 3600000,
  GOOGLE_PLACES_CACHE_TTL_MS: 86400000,
  SPAM_CACHE_TTL_MS: 3600000,
  CDR_SYNC_INTERVAL_MS: 300000,

  // Limites
  LOG_BUFFER_MAX_SIZE: 300,
  SIRENE_CACHE_MAX_SIZE: 500,
  GOOGLE_PLACES_CACHE_MAX_SIZE: 500,
  SPAM_CACHE_MAX_SIZE: 1000,
  DEFAULT_PAGE_LIMIT: 50,
  DEFAULT_CALL_HISTORY_LIMIT: 1000,
  MAX_PAGE_LIMIT: 200,
  DEFAULT_SEARCH_LIMIT: 20,
  MAX_CALLS_TO_RESOLVE: 200,
  SPAM_BLOCK_THRESHOLD: 7,
  MIN_SEARCH_NAME_LENGTH: 2,
  MIN_PHONE_LENGTH: 6,
  MIN_PHONE_DIGITS_LENGTH: 5,
  AUTO_CREATE_LOCK_DELAY_MS: 10000,

  // Codes HTTP
  HTTP_OK: 200,
  HTTP_BAD_REQUEST: 400,
  HTTP_UNAUTHORIZED: 401,
  HTTP_FORBIDDEN: 403,
  HTTP_NOT_FOUND: 404,
  HTTP_TOO_MANY_REQUESTS: 429,
  HTTP_INTERNAL_ERROR: 500,
  HTTP_SERVICE_UNAVAILABLE: 503,

  // Rate Limiting
  RATE_LIMIT_WINDOW_MS: 15 * 60 * 1000,
  RATE_LIMIT_MAX_REQUESTS: 5000,

  // Validation
  SIREN_LENGTH: 9,
  SIRET_LENGTH: 14,
  MAX_INTERNAL_NUMBER_LENGTH: 5,
  SIREN_REGEX: /^\d{9}$/,
  SIRET_REGEX: /^\d{14}$/,

  // Whisper
  WHISPER_MAX_DURATION_SEC: 600,
  WHISPER_DEFAULT_MODEL: 'tiny',
  WHISPER_DEFAULT_LANGUAGE: 'fr',

  // UCM CDR
  UCM_SUB_CDR_KEYS: ['sub_cdr_1', 'sub_cdr_2', 'sub_cdr_3', 'sub_cdr_4'],

  // WebSocket
  WS_DEFAULT_PATH: '/ws',

  // Serveur
  DEFAULT_SERVER_PORT: 3000,
  DEFAULT_UCM_WEBHOOK_PORT: 8088,
  DEFAULT_UCM_WEB_PORT: 8089,
};
